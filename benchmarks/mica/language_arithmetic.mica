// Arithmetic over a loop.
//
// Per-call workload: 100k iterations of integer multiply-add.
verb bench()
  let index = 0
  let total = 0
  while index < 100000
    total = total + index * 3
    index = index + 1
  end
  return total
end
